include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Regenerated by tool/bootstrap.sh; not ours to lint.
    - android/**
    - ios/**

linter:
  rules:
    - prefer_single_quotes
    - avoid_print
    - use_super_parameters
